# Orchard Tracker

Spray management app for orchard growers. Built to be testable end-to-end.

## Structure
- `apps/api` — Fastify + Prisma backend
- `apps/web` — React + Vite frontend
- `packages/shared` — Shared TypeScript types

## Dev
See individual app READMEs. Run `npm install` at the root first.
